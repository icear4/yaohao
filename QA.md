# 残响回廊 ECHO RIFT — QA 报告与项目总览

> 生成时间：2026-09-22　·　本轮只做测试与缺陷修复，**未新增任何玩法系统**

---

## 一、本轮 QA 结论

| 测试 | 项数 | 结果 |
|---|---|---|
| `devtests/_qa.js`（全面功能） | 86 | **全 PASS** |
| `devtests/_qa_run.js`（完整通关，7 个 Seed） | 11 × 7 | **全 PASS** |
| `devtests/_smoke3~14`（历史回归） | 295 | **全 PASS** |
| `devtests/_soak.js`（随机输入长时间浸泡） | 15101 帧 | 无异常 |
| `devtests/_diag_stuck.js`（敌人可达性诊断） | 904 只敌人 | 无死点 |

通关验证（机器人从「新游戏」打到最终 Boss 并结算）：

| Seed | 击杀 | 用时 | 金币 | 通关 Boss 路线 |
|---|---|---|---|---|
| RUNFULL | 249 | 503s | 5590 | 守望者→铁律摆钟→腐殖母冠→折射歌伶→**坍缩星核** |
| ALPHA | 257 | 485s | 6427 | 守望者→铁律摆钟→腐殖母冠→沙丘巨颚→**坍缩星核** |
| BRAVO | 282 | 395s | 6282 | 守望者→铁律摆钟→腐殖母冠→万伏主教→**坍缩星核** |
| CHARLIE | 284 | 336s | 6901 | 守望者→熔渣铸匠→腐殖母冠→万伏主教→**坍缩星核** |
| DELTA | 303 | 436s | 8263 | 守望者→铁律摆钟→腐殖母冠→沙丘巨颚→**坍缩星核** |
| ECHO | 262 | 412s | 5964 | 守望者→铁律摆钟→经纬织者→折射歌伶→**坍缩星核** |
| FOXTROT | 222 | 392s | 6077 | 守望者→熔渣铸匠→经纬织者→折射歌伶→**坍缩星核** |

七个 Seed 全部走完 5 层、击杀最终 Boss、进入 victory 结算，且结算数据（SEED / 路线 / Boss / 击杀 / 用时 / 金币）完整。

---

## 二、本轮发现并修复的问题

| # | 问题 | 定位 | 修复 |
|---|---|---|---|
| 1 | 实体被极端位移（强击退 / 障碍推挤 / 传送类技能）甩到**墙体外侧**后会永久卡死——墙体解算只在「贴墙」时生效，够不到远处 | `Room.clampEntity` 只做墙体分离，没有兜底 | 在 `clampEntity` 末尾加竞技场兜底：实体跑到视口外时拉回 `ARENA` 内。不影响正常贴墙与门洞通行 |
| 2 | 敌人出生点没有排除**实心环境障碍**，约 1% 的敌人会直接生在石柱 / 齿轮 / 相位柱里，子弹被障碍吸收 → 极难命中 | `Room._spawnPoint` 只检查玩家距离与同类间距 | 出生点增加 `hazards.hitTest` 排除；兜底落点也改成环形搜索不撞障碍的点 |

> 另有 3 处是**测试脚本自己的断言写错**（房间缓存 key 格式、秘室逐间统计、裂缝需 3 发才碎），已修正测试而非改游戏。

诊断脚本 `_diag_stuck.js` 额外确认：**904 只敌人中「被障碍完全包围、任何角度都打不到」的数量为 0**，不存在死点。

---

## 三、文件结构

```
ember-rift/
├── index.html                 页面骨架（#stage 1280×720 + UI 层 + 遮罩面板）
├── css/style.css              全部样式（HUD 按钮 / 遮罩面板 / 角色卡 / 图鉴 / 触屏）
├── build.js                   把 index.html + css + js 内联打包成单文件
├── echo-rift-standalone.html  单文件版（602 KB，双击即玩）
│
├── js/                        模块源码（共 19274 行，按 index.html 顺序加载）
│   ├── utils.js        153   math / Rng / 常量（WALL_T / ARENA / VIEW_W…）
│   ├── input.js        109   键鼠输入（含世界坐标换算）
│   ├── touch.js        144   移动端虚拟双摇杆 + 屏幕按钮
│   ├── collision.js    133   圆-圆 / 圆-矩形解算 + 均匀网格 SpatialGrid
│   ├── juice.js        256   手感层：HitStop / Perf 自适应 / 投射物视觉档案 / Juice 特效
│   ├── particles.js    315   粒子系统（合批绘制 + 预算上限）
│   ├── projectile.js   307   投射物（对象池 + 环形拖影缓冲 + 多种形状/轨迹）
│   ├── player.js       602   玩家（移动 / 射击 / 受伤 / 击退 / 角色能力钩子）
│   ├── items.js        642   44 件道具 + 22 个组合 + Build 计算
│   ├── buff.js         106   增益 / 诅咒
│   ├── props.js        501   互动物件：宝箱 / 祭坛 / 货架 / 传送门 / 遗物 / 金币堆
│   ├── shop.js         388   商栈库存与价格
│   ├── events.js       301   13 个房间事件
│   ├── secret.js       190   墙面裂缝 → 隐藏房
│   ├── enemy.js       2032   敌人基类 + Boss 基类 + 危险区 / 激光 + 工厂
│   ├── enemy_roster.js 2773  26 种普通敌人（召唤 / 分裂 / 治疗 / 护盾 …）
│   ├── bosses.js       2812  11 位 Boss（三阶段 / 每位数套招式 / 专属特殊技）
│   ├── chapters.js     1354  5 章配置 + 章节背景 + 7 类环境障碍
│   ├── meta.js         1363  存档 / 统计 / 解锁 / 角色 / 设置（localStorage）
│   ├── map.js          398   程序化地图（生长式，天然连通）
│   ├── spawn.js        242   编队生成器（预算 + 硬约束）
│   ├── room.js         775   房间：墙体 / 门 / 波次 / 物件 / 障碍 / 演出
│   ├── ui.js          1294  HUD + 小地图 + 遮罩面板（角色 / 统计 / 图鉴）
│   ├── game.js        1384  主循环 / 状态机 / 换房换层 / 全局事件
│   └── main.js          75   启动入口
│
└── devtests/                  headless 自检（node 直接跑）
    ├── _smoke3~14.js          分阶段回归（地图 / 道具 / 敌人 / Boss / 章节 / Meta / 手感 / UI）
    ├── _qa.js                 ★ 本轮新增：全面 QA（86 项，分 9 个板块）
    ├── _qa_run.js             ★ 本轮新增：完整通关流程（机器人，可指定 SEED 环境变量）
    ├── _diag_stuck.js         ★ 本轮新增：敌人可达性 / 出生点诊断
    └── _soak.js               随机输入长时间浸泡（15101 帧）
```

---

## 四、核心系统说明

**主循环（`game.js`）**
固定逻辑步长 `dt`（上限 0.05s，防止卡顿后物理爆炸）→ 输入 → 玩家 → 房间 → 投射物 → 特效 → 粒子 → 绘制。状态机：`title → playing ⇄ paused → gameover / victory`。`HitStop` 在世界时间上做短暂压缩（击杀定格），`Perf` 只在主循环采样（不进 `update`，避免污染确定性）。

**地图（`map.js`）**
生长式生成：起点固定在最左列 → 主路径向右推进到最右列 → 末端即 Boss 房 → 主路径上长出 2~4 条支线 → 四方向相邻即连通 → `_ensureBossTerminal` 保证 Boss 始终最远 → 最后挂隐藏房。相同 Seed + 层数 → 完全相同地图。

**房间（`room.js`）**
房间实例按 `floor#c,r` 缓存，重入复用（敌人 / 宝箱 / 状态都不会刷新）。战斗房：进门锁门 → 按波次放怪（每只派生独立 Rng）→ 清完开门 → 掉传送门 / 遗物。墙体分「开 / 关」两套矩形；环境障碍对玩家与敌人同等生效，且**先预警再生效**。

**敌人（`enemy.js` + `enemy_roster.js`）**
基类统一处理：生成动画、受击白闪 + 击退、血条（带延迟层）、状态（燃烧 / 冰冻 / 中毒 / 减速）、章节缩放、词缀。子类只写 AI。Boss 基类额外提供三阶段（70% / 40%）、地面危险区、激光、召唤、专属死亡动画。

**伤害与 Build（`items.js`）**
`Build.recompute()`：属性 → 机制 → 组合 → 写回玩家。组合在机制层数或属性阈值达标时自动成立。角色 mods 必须在 `new BuffSystem` 之后重算（否则会被覆盖）。

**手感（`juice.js`）**
HitStop 定格、冲击波、全屏闪色、道具浮现卡、命中/暴击特效、13 种投射物视觉档案、自适应性能档位（FPS 掉档时先关拖尾发射器，再降粒子预算）。

**Meta（`meta.js`）**
`localStorage['echo-rift-meta-v1']`，只存：解锁、统计、设置。**不存**当前 Run 的 Build / 金币 / 层数 / 血量。解锁只拓宽随机池，不给局外数值加成。

---

## 五、已完成内容

- **核心**：移动 / 射击 / 碰撞 / 伤害 / 护甲 / 无敌帧 / 死亡 / 重开，全部有回归覆盖
- **随机地图**：生长式生成 + Seed 可复现 + 房间缓存 + 门（闸门开合动画）
- **房间类型**：起点 / 战斗 / 宝库 / 精英 / 商栈 / 异象 / 首领 / **秘室**（击碎墙上裂缝发现）
- **道具**：44 件（6 件需解锁）+ 22 个组合 + 堆叠上限 + UI（色块条 + 悬停/固定展开看说明）
- **敌人**：26 种普通敌人（含召唤 / 分裂 / 治疗 / 护盾 / 自爆 / 炮台等）+ 11 位 Boss（三阶段、≥4 招、专属特殊技、召唤、死亡动画）
- **五章流程**：废弃庭院 → 机械矿井 → 腐化森林 → 虚空遗迹 → 星界核心，难度五维递增（HP / 攻击节奏 / 移速 / 弹幕复杂度 / 精英渗透），每层独立视觉、敌人组合、Boss、环境障碍
- **Meta**：5 位角色（不同起手式）、15 条解锁（角色 / 道具 / 事件 / Boss）、统计页、解锁图鉴（可滚动）
- **手感**：受伤闪烁 / 无敌环 / 击退 / 方向冲击屏震 / 开火反馈 / 暴击特效；敌人受击闪白、血条延迟层、死亡粒子与爆炸
- **性能**：投射物对象池、空间网格宽阶段、粒子合批、自适应降档
- **设置**：屏幕抖动 与 画面闪烁 各自 关/弱/中/强 四档，写入存档
- **移动端**：虚拟双摇杆 + 屏幕按钮 + 等比缩放 + 竖屏提示
- **单文件版**：`node build.js` → `echo-rift-standalone.html`

---

## 六、已知问题

1. **敌人在实心障碍边缘会被判定为「接触」**：`Collision.circleCircle` 用 `<=`，被障碍推挤到恰好相切时会算作命中障碍（约 0.7% 采样帧）。表现无感知（敌人仍能被绕过角度打到），未改动以免波及全部碰撞语义。
2. **机器人式玩法会被地形卡住**：站在柱子正后方对着掩体射击时子弹永远被吸收，需要玩家自己绕位——属于设计内的「掩体」，但新手可能感到困惑（可考虑给被吸收的子弹加更明显的反馈）。
3. **商栈买不起的商品不会消失**：会一直留在货架上（已确认不会死循环，只是视觉上一直可点）。
4. **粒子上限是全局预算**：极端弹幕场景下（Perf 降档到 300）少量远处特效会被丢弃。
5. **画面闪烁关闭后仍保留 35% 受击白闪**：刻意保留的可读性地板值，若想彻底无闪需改 `hitFlashMul` 公式。
6. **headless 测试无法覆盖真实渲染**：所有绘制走 Proxy 假 ctx，视觉问题（错位 / 遮挡 / 颜色）仍需人工过目。

---

## 七、后续可以扩展的内容

- **局内成长深度**：道具升级（同一件的第二层效果）、可拆卸 / 替换道具、诅咒道具的取舍机制
- **更多 Boss 机制**：双人组 Boss、可破坏部位、阶段之间穿插小怪波次
- **地图多样性**：特殊地形房（传送阵 / 移动平台 / 单向门）、层间支线选择、每日挑战 Seed
- **Meta 深度**：局外解锁「开局携带道具」「房间修饰符」「难度选项（给愿意受苦的玩家）」
- **表现层**：敌人死亡残影、Boss 入场演出、章节过场、音效（当前完全无声）
- **可访问性**：色弱模式、按键自定义、更细的可读性选项（伤害数字 / 命中标记 独立开关）
- **工程**：把 `devtests` 的 DOM/Canvas mock 抽成公共 harness（现在每个脚本各写一份），并接入 CI
